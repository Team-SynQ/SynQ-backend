config.stopBubbling = true

# @RequiredArgsConstructor 가 만드는 생성자에 필드의 @Qualifier 를 복사한다.
# 복사하지 않으면 같은 타입의 빈이 둘 이상일 때 필드에 적어둔 @Qualifier 가 조용히 무시되고
# NoUniqueBeanDefinitionException 으로 기동이 깨진다.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
