# 실제 OpenAI 1시간 Live Context·자동 힌트 통합 테스트

## 실행 결과

| 항목 | 결과 |
| --- | --- |
| 회의 ID | 5 |
| 전사 세그먼트 | 180개 (20초 간격, 총 60분) |
| Live Context 갱신 | 90회 (2개 세그먼트 또는 최대 10초 대기) |
| 자동 3-hint 저장 | 8개 |
| Live Context 평균 지연 | 4232ms |
| Live Context 최대 지연 | 19577ms |
| 자동 3-hint 평균 표시 지연 | 7538ms |
| 자동 3-hint 최대 표시 지연 | 8114ms |
| 실행 시작 시각 | 2026-08-11T07:04:01.805747Z |
| 실행 소요 시간 | PT6M35.915746S |

> 이 평가는 `RUN_OPENAI_ONE_HOUR_INTEGRATION_TEST=true`일 때만 실행됩니다.
> 실제 OpenAI Responses API를 호출했으며, API 키는 기록하지 않습니다.

---

## OpenAI Live Context 요청

- 모델: `gpt-5.6-luna`
- Reasoning effort: `low`
- 최대 출력 토큰: `1200`
- 응답 형식: strict JSON Schema `meeting_live_context`
- 호출 방식: 기존 누적 맥락 + 새 확정 전사 2개를 `input`으로 전달

### 첫 번째 요청의 새 확정 전사

- `segmentId=367, sequence=0` 00:00 진행 공유: 온보딩 개선 작업의 현황과 사용자 이탈률을 확인했고, 다음 작업 범위를 논의 중입니다.
- `segmentId=368, sequence=1` 00:20 진행 공유: 온보딩 개선 작업의 현황과 사용자 이탈률을 확인했고, 다음 작업 범위를 논의 중입니다.

### 마지막 요청의 새 확정 전사

- `segmentId=545, sequence=178` 59:20 진행 공유: 온보딩 개선 작업의 현황과 사용자 이탈률을 확인했고, 다음 작업 범위를 논의 중입니다.
- `segmentId=546, sequence=179` 59:40 결정: 온보딩 개선안을 이번 분기 최우선 과제로 확정합니다. 민규는 API 구현을, 서윤은 QA 시나리오 검토를 담당하고 금요일까지 진행 상황을 공유합니다.

---

## 실제 OpenAI 응답

### 첫 번째 Live Context 응답

- 마지막 반영 세그먼트: `368` (sequence `1`)
- 누적 요약: 온보딩 개선 작업의 진행 현황과 사용자 이탈률을 확인했으며, 다음 작업 범위를 논의하고 있다.
- 현재 주제: 온보딩 개선 작업의 현황과 사용자 이탈률을 검토하고 다음 작업 범위를 논의하는 중
- 결정 사항: 없음
- 액션 아이템: 없음
- 미해결 질문: 다음 작업 범위를 어떻게 정할 것인가? / 사용자 이탈률 검토 결과는 무엇인가?
- 자동 힌트 판단: `shouldGenerate=false`, `targetSegmentId=null`, `importance=0`, 사유: 없음


### 마지막 Live Context 응답

- 마지막 반영 세그먼트: `546` (sequence `179`)
- 누적 요약: 온보딩 개선안은 이번 분기 최우선 과제로 유지되며, 민규가 API 구현을, 서윤이 QA 시나리오 검토를 담당한다. 두 사람은 금요일까지 진행 상황을 공유한다. 온보딩 개선 작업 현황과 사용자 이탈률을 확인했으며, 다음 작업 범위는 아직 논의 중이다. QA 인력 확보가 늦어지면 베타 일정이 지연될 수 있으므로 다음 회의에서 대체 일정과 작업 범위를 확인해야 한다. 새 발화에서는 기존 결정과 진행 상황이 재확인되었으며, 추가적인 결정·담당자 지정·일정 변경은 없다.
- 현재 주제: 온보딩 개선 작업의 다음 작업 범위 논의 및 API 구현·QA 시나리오 검토와 QA 리스크를 반영한 베타 일정 대안 논의
- 결정 사항: 온보딩 개선안을 이번 분기 최우선 과제로 확정한다. / 민규는 API 구현을 담당한다. / 서윤은 QA 시나리오 검토를 담당한다. / 민규와 서윤은 금요일까지 진행 상황을 공유한다.
- 액션 아이템: 민규가 온보딩 개선안 API 구현을 담당한다. / 서윤이 온보딩 개선안 QA 시나리오 검토를 담당한다. / 민규와 서윤이 금요일까지 각 담당 업무의 진행 상황을 공유한다. / 다음 회의에서 QA 인력 확보 지연에 대비한 대체 일정과 작업 범위를 확인한다.
- 미해결 질문: 사용자 이탈률 검토 결과는 무엇인가? / QA 인력 확보가 지연될 경우 대체 일정과 작업 범위는 어떻게 정할 것인가? / 최우선 과제로 확정된 온보딩 개선안의 구체적인 작업 범위는 어디까지인가?
- 자동 힌트 판단: `shouldGenerate=false`, `targetSegmentId=null`, `importance=0`, 사유: 없음


---

## 실제 자동 3-hint 응답

- 대상 세그먼트: `380`
- 중요도: `85` / 판단 사유: QA 인력 확보 지연으로 베타 일정이 밀릴 수 있는 리스크가 확인되었고, 다음 회의에서 대체 일정과 범위를 확인해야 합니다.
- 의미: 현재 온보딩 개선 작업과 사용자 이탈률을 검토한 뒤 다음 범위를 논의하는 단계입니다. QA 인력 확보가 늦어지면 테스트가 필요한 작업이 지연되어 베타 일정도 밀릴 수 있으므로, 다음 회의에서 대체 일정과 작업 범위를 함께 결정해야 한다는 의미입니다.
- 내 영향: QA 인력 확보 지연이 베타 일정에 영향을 줄 수 있으므로, 다음 작업 범위와 일정 대안이 정해질 때 QA 의존성이 큰 업무의 우선순위와 조정 가능 범위를 확인해야 합니다.
- 팀 질문: QA 인력 확보가 지연될 경우를 대비해, 베타 일정을 유지하는 축소 범위와 일정을 연기하는 경우의 대안을 각각 무엇으로 정할까요?


- 대상 세그먼트: `386`
- 중요도: `92` / 판단 사유: 이번 분기 최우선 과제와 담당자(API 구현·QA 시나리오 검토), 진행 상황 공유 기한이 새로 확정되었습니다.
- 의미: 팀은 온보딩 개선안을 이번 분기의 최우선 과제로 공식 확정했습니다. 이에 따라 민규는 API 구현을, 서윤은 QA 시나리오 검토를 담당하며 두 사람은 금요일까지 진행 상황을 공유하기로 했습니다. 앞서 확인한 사용자 이탈률과 현재 논의 중인 작업 범위를 실제 실행 계획으로 구체화하는 결정입니다.
- 내 영향: 사용자의 역할과 관점은 제공되지 않아 개인별 영향은 특정할 수 없습니다. 다만 이번 분기 온보딩 개선이 최우선 과제가 되었으므로, 관련 업무를 맡은 참여자는 API 구현·QA 시나리오 검토 및 금요일 진행 상황 공유와의 연계를 확인해야 합니다.
- 팀 질문: 금요일까지 공유할 API 구현 및 QA 시나리오 검토의 구체적인 산출물과, QA 인력 확보가 지연될 경우의 대체 일정·작업 범위를 어떻게 정할까요?


- 대상 세그먼트: `408`
- 중요도: `78` / 판단 사유: QA 인력 확보 지연으로 베타 일정이 밀릴 수 있는 리스크가 확인되었고, 대체 일정과 작업 범위 검토가 필요함
- 의미: 온보딩 개선안은 이번 분기 최우선 과제이며, 민규가 API 구현을 맡고 서윤이 QA 시나리오를 검토하기로 했습니다. 이 발화는 QA 인력 확보 지연이 베타 일정의 위험 요인임을 알리고, 다음 회의에서 대체 일정과 작업 범위를 정해야 한다는 의미입니다. 구체적인 대안은 아직 결정되지 않았습니다.
- 내 영향: QA 인력 확보가 지연되면 베타 출시 일정과 작업 범위 결정에 영향을 줄 수 있습니다. 현재 역할과 관점이 정해지지 않아 구체적인 개인별 영향은 판단하기 어렵습니다.
- 팀 질문: QA 인력 확보가 늦어질 경우를 대비해 어떤 대체 일정과 작업 범위를 선택할지, 그리고 그 결정 기준은 무엇인가요?
