# 이 파일을 .env 로 복사한 뒤 실제 값을 채운다. .env 는 커밋하지 않는다.

GEMINI_API_KEY=dummy

OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-nano
OPENAI_BASE_URL=https://api.openai.com/v1

# 실제 OpenAI 연결 테스트를 실행할 때만 true 로 변경한다.
RUN_OPENAI_SMOKE_TEST=false
